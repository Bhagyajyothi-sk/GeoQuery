# GeoQueryAI — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Pydantic ───────────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── HTTP client (geocoding) ────────────────────────────────────────────────────
requests>=2.31.0

# ── Geospatial (STAC + COG) ───────────────────────────────────────────────────
pystac-client>=0.7.0
planetary-computer>=1.0.0
rasterio>=1.3.0
numpy>=1.26.0
shapely>=2.0.0

# ── AI / ML (install separately — not required to start the server) ───────────
# google-generativeai    # Gemini — plug into services/ai/interface.py
# torch                  # RemoteCLIP dependency
# faiss-cpu              # FAISS — plug into services/search/interface.py
# (RemoteCLIP has no PyPI package; install from source per your implementation)
